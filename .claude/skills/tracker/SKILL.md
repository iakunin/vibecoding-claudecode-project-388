---
name: tracker
description: Use when нужно проверить цены сразу по всему отслеживаемому списку товаров — «проверь цены», «запусти трекер», «что изменилось по ценам». Обходит зашитый внутри список URL, для каждого вызывает скилл extract-price, сравнивает свежий прогон с предыдущим из репозитория tracker-data по правилам значимости из KNOWLEDGE.md, сохраняет прогон как YYYY-MM-DD.json и присылает сводку значимых изменений в Telegram. Отдаёт только значимые изменения, а не весь прайс.
---

# tracker

Один запуск = один прогон по всему списку отслеживаемых товаров.

Зачем: подгадать выгодный момент покупки. Смотреть цены имеет смысл целиком по списку, а не по одному товару — так видно, какая позиция просела относительно соседних.

## Контракт

**Вход:** ничего. Список зашит в этом файле, перечислять ссылки при запуске не нужно.

**Выход:** список **значимых изменений** относительно предыдущего прогона — по правилам из `KNOWLEDGE.md`. Не весь прайс. Нет значимых изменений — так и скажи одной строкой.

Полную таблицу цен показывай, только если это первый прогон (сравнивать не с чем) или пользователь попросил её явно.

**Побочные эффекты:** прогон сохраняется в репозиторий `tracker-data` как `YYYY-MM-DD.json`, сводка значимых изменений уходит в Telegram через `send.py`.

## Список отслеживаемых товаров

Мониторные колонки ADAM (магазин united-music.ru):

| Товар | URL |
|---|---|
| ADAM A4V (чёрный) | https://united-music.ru/ru/adam-a4v.html |
| ADAM A4V (белый) | https://united-music.ru/ru/adam-a4v-white.html |
| ADAM A7V (чёрный) | https://united-music.ru/ru/adam-a7v.html |
| ADAM A7V (белый) | https://united-music.ru/ru/adam-a7v-white.html |
| ADAM A77H | https://united-music.ru/ru/adam-a77h.html |
| ADAM A8H B-side | https://united-music.ru/ru/adam-a8h-b-side.html |
| ADAM A8H A-side | https://united-music.ru/ru/adam-a8h-a-side.html |

Это единственное место, где список хранится. Добавить или убрать товар — отредактировать таблицу выше, больше ничего менять не нужно.

## Алгоритм

### 1. Пройти по списку

Для **каждого** URL из таблицы вызови скилл `extract-price` — он принимает ровно один URL и возвращает объект `{regular_price, sale_price, has_credit}`, а при неудаче — тот же объект с `null` и строку с причиной.

`tracker` **не содержит собственной логики извлечения цены**: не ходит curl'ом за страницей сам, не парсит HTML, не знает про `span.price` и ловушку `itemprop="price"`. Всё это живёт в `extract-price`. Если парсинг сломался — чинить надо `extract-price`, а не этот файл.

Товары независимы — вызовы можно делать параллельно.

Пропускать URL нельзя: в таблице должны оказаться все строки списка, включая те, где цену получить не удалось.

### 2. Собрать таблицу прогона

Это внутренний промежуточный результат: из него получится и запись в хранилище, и diff. Пользователю целиком она показывается только на первом прогоне или по отдельной просьбе.

Одна строка на товар, поля — ровно те, что вернул `extract-price`:

```markdown
| Товар | URL | regular_price | sale_price | has_credit |
|---|---|---|---|---|
| ADAM A4V (чёрный) | https://united-music.ru/ru/adam-a4v.html | 48600 | — | да |
| ADAM A4V (белый) | https://united-music.ru/ru/adam-a4v-white.html | 50100 | — | да |
```

Оформление ячеек:

- `regular_price` / `sale_price` — целое число рублей как есть, без пробелов-разделителей и без `р.`.
- `sale_price: null` → `—` (скидки нет).
- `has_credit` → `да` / `нет`.
- Любое поле `null` из-за ошибки → `—`.

Порядок строк — как в списке выше. Не сортируй по цене: строки должны совпадать со списком, чтобы прогоны можно было сравнивать между собой.

### 3. Прочитать предыдущий прогон

Сравнение делается **до** сохранения свежего прогона — иначе он перезапишет файл за сегодня и сравнивать будет не с чем.

Возьми список файлов репозитория и выбери последний прогон по дате, **исключив файл за сегодня**:

```bash
DATE=$(date +%F)
TOKEN=$(gh auth token)
PREV=$(curl -sS -H "Authorization: Bearer $TOKEN" \
  https://api.github.com/repos/iakunin/tracker-data/contents \
  | python3 -c '
import sys, json, re
names = [f["name"] for f in json.load(sys.stdin)
         if re.fullmatch(r"\d{4}-\d{2}-\d{2}\.json", f["name"])]
names = [n for n in names if n != "'"$DATE"'.json"]
print(max(names) if names else "")')

if [ -n "$PREV" ]; then
  curl -sS -H "Authorization: Bearer $TOKEN" -H "Accept: application/vnd.github.raw" \
    "https://api.github.com/repos/iakunin/tracker-data/contents/$PREV"
fi
```

Имена файлов — `YYYY-MM-DD`, поэтому «последний по дате» — это просто максимум по строке, разбирать даты не нужно. Всё, что не подходит под шаблон (`README.md` и прочее), в расчёт не берётся.

`PREV` пустой — прошлых прогонов нет, это первый запуск: сравнивать не с чем, переходи к сохранению и скажи об этом пользователю.

### 4. Посчитать diff и отсеять незначимое

Сопоставь товары **по `url`** (не по имени — имя можно переписать в списке) и сравни поля `regular_price`, `sale_price`, `has_credit`.

Правила значимости — в [`KNOWLEDGE.md`](../../../KNOWLEDGE.md) в корне репозитория проекта. **Прочитай его при каждом прогоне** и применяй оттуда: пороги падения и роста, трактовку появления/исчезновения скидки и рассрочки, новые и пропавшие товары, поведение при ошибках. Не держи пороги в голове и не дублируй их здесь: правила живут в одном месте, чтобы их можно было менять, не трогая скилл.

Изменение, не прошедшее порог, из результата выбрасывается — оно не попадает ни в вывод, ни в будущее уведомление. В сохранённый JSON попадают все товары: фильтруется отчёт, а не история.

Формат вывода — короткие строки, по одной на значимое изменение, с конкретными числами:

```
ADAM A4V (чёрный): 49 500 → 48 600 ₽ (−1.8%)
ADAM A7V (чёрный): скидка закончилась, 62 000 → 67 500 ₽
ADAM A77H: появилась рассрочка
ADAM A8H A-side: новый товар в списке, 162 000 ₽
```

Значимых изменений нет — одна строка «Значимых изменений нет», и всё. Не подменяй пустой diff полной таблицей цен.

### 5. Сохранить прогон в tracker-data

История прогонов живёт отдельно от кода скиллов — в приватном репозитории `iakunin/tracker-data`, ветка `main`.

Схема хранения: **один прогон — один файл `YYYY-MM-DD.json` в корне репозитория**. Имя файла — дата прогона по местному времени, `date +%F`.

Содержимое — та же таблица прогона, только в JSON:

```json
{
  "date": "2026-09-20",
  "items": [
    {
      "name": "ADAM A4V (чёрный)",
      "url": "https://united-music.ru/ru/adam-a4v.html",
      "regular_price": 48600,
      "sale_price": null,
      "has_credit": true,
      "error": null
    }
  ]
}
```

- `items` — все товары списка, в том же порядке, что и в таблице. Не выкидывай товар, по которому цену получить не удалось: у него просто `null` в ценах.
- Значения — как их вернул `extract-price`: целые числа и `null`, без оформления (`—` и `да`/`нет` живут только в markdown-таблице).
- `error` — причина строкой (`"HTTP 404"`, `"таймаут"`), если цену получить не удалось, иначе `null`. Без этого поля следующий прогон не отличит упавшую сеть от реально пропавшей цены и соврёт про изменение.

Запись идёт через GitHub Contents API одним коммитом, без `git clone` и `git push`:

```bash
DATE=$(date +%F)
RUN="$TMPDIR/$DATE.json"   # сюда положен JSON прогона
TOKEN=$(gh auth token)
API="https://api.github.com/repos/iakunin/tracker-data/contents/$DATE.json"

CONTENT=$(base64 < "$RUN" | tr -d '\n')
SHA=$(curl -sS -H "Authorization: Bearer $TOKEN" "$API" \
  | python3 -c 'import sys,json; print(json.load(sys.stdin).get("sha",""))')

if [ -n "$SHA" ]; then
  BODY="{\"message\":\"Run $DATE\",\"content\":\"$CONTENT\",\"sha\":\"$SHA\"}"
else
  BODY="{\"message\":\"Run $DATE\",\"content\":\"$CONTENT\"}"
fi

curl -sS -X PUT -H "Authorization: Bearer $TOKEN" -H "Accept: application/vnd.github+json" \
  "$API" -d "$BODY"
```

Три грабли, каждая проверена на этом окружении:

1. **Файл и запись — в одном вызове shell.** `$TMPDIR` у песочницы и у команд вне её разный. Если записать JSON одной командой, а `base64` вызвать другой, прочитается пустота и в репозиторий уедет файл нулевого размера — причём GitHub ответит успехом.
2. **`gh api --method PUT` здесь падает** с `tls: failed to verify certificate: x509: OSStatus -26276`, хотя `gh api` на чтение работает. Отсюда `curl` с токеном из `gh auth token`. Токен в вывод не печатать.
3. **`${SHA:+-f sha=...}` в zsh не разворачивается в отдельные аргументы** — `sha` молча не доедет, и придёт `422`. Отсюда явный `if`.

`sha` нужен, только если файл за сегодня уже есть — при повторном прогоне в тот же день. Без него GitHub ответит `422`, с ним прогон перезапишется новым коммитом.

Почему не GitHub MCP, хотя задание шага про него: сервер `github` в `/mcp` не подключён, fine-grained токена нет. Задание шага 2 прямо разрешает этот запасной путь — «сохраняйте прогоны обычным git с тем же токеном». Схема хранения от этого не меняется: файл `YYYY-MM-DD.json` в корне `tracker-data`, один прогон — один коммит, без локального клона. Появится MCP — меняется только этот блок команд.

Прогон за прошлую дату задним числом не пиши: имя файла — всегда сегодняшняя дата.

После записи убедись, что ответ содержит `commit.sha`, и покажи ссылку на коммит под таблицей.

### 6. Отправить сводку в Telegram

Отправка — **после** сохранения: прогон должен быть в истории, даже если Telegram недоступен.

Сводка отправляется **каждый прогон**, в том числе когда значимых изменений нет — так видно, что трекер жив и отработал.

```bash
python3 /Users/iakunin/Code/github.com/iakunin/vibecoding-claudecode-project-388/send.py "$SUMMARY"
```

`send.py` берёт `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` из окружения или из `.env` рядом с собой. Токен в вывод не печатать и в репозиторий не коммитить: `.env` закрыт `.gitignore`.

Формат сводки — заголовок с датой и по одной строке на изменение. Значок показывает тип события, чтобы строки читались с одного взгляда:

```
📉 Изменения цен 2026-09-20

🔻 ADAM A4V (чёрный): 49 500 → 48 600 ₽ (−1.8%)
🔺 ADAM A8H B-side: 155 000 → 162 000 ₽ (+4.5%)
🏷 ADAM A7V (чёрный): скидка закончилась, 62 000 → 67 500 ₽
💳 ADAM A77H: появилась рассрочка
➕ ADAM A8H A-side: новый товар, 162 000 ₽
➖ ADAM T7V: пропал из списка
```

| Значок | Событие |
|---|---|
| 🔻 | Цена упала |
| 🔺 | Цена выросла |
| 🏷 | Скидка появилась или закончилась |
| 💳 | Изменилась рассрочка |
| ➕ / ➖ | Товар добавился или пропал |
| ⚠️ | Цену не удалось проверить |

Нет значимых изменений — одно короткое сообщение:

```
✅ Цены на месте, значимых изменений нет — 2026-09-20
```

Первый прогон (сравнивать не с чем) — снимок цен целиком:

```
📸 Первый прогон — снимок цен

ADAM A4V (чёрный) — 48 600 ₽ 💳
ADAM A7V (чёрный) — 67 500 ₽ 💳
```

Цены в сообщении — с пробелом-разделителем тысяч и знаком `₽`: сообщение читает человек, а не парсер. В JSON-прогоне по-прежнему голые числа.

Сообщение отправляется **одно на прогон**, а не по сообщению на товар.

Не дошло — скажи об этом прямо и покажи текст сводки в ответе. Провал доставки не отменяет прогон: он уже сохранён в `tracker-data`.

### 7. Отчитаться об ошибках

Если по какому-то товару `extract-price` вернул `null` — строка всё равно остаётся в таблице с прочерками, а под таблицей отдельным списком перечисли причины:

```
Не удалось получить цену:
- ADAM A77H — HTTP 404, страница не найдена
```

Не выдумывай цену, не подставляй значение соседнего товара и не бери число из прошлого прогона.

Если не удалось получить вообще ничего — всё равно выведи таблицу с прочерками и список причин. Пустой ответ — не результат.

Прогон с частично пустыми ценами всё равно сохраняется в `tracker-data`: пропуск в истории хуже, чем запись с `null`.

Если упала сама запись в репозиторий (нет сети, `gh` не авторизован, `403`) — скажи об этом прямо и покажи собранный JSON в ответе, чтобы прогон не потерялся. Не делай вид, что сохранение прошло.

## Частые ошибки

| Ошибка | Что делать |
|---|---|
| Обошёл один-два URL и отчитался | Прогон = весь список за один запуск |
| Спросил у пользователя, какие ссылки проверять | Список зашит здесь, спрашивать нечего |
| Начал парсить HTML сам | Вызывай `extract-price`, логика цены живёт там |
| Выкинул из таблицы товар, у которого не нашлась цена | Строка остаётся, в полях `—`, причина под таблицей |
| Отсортировал таблицу по цене | Порядок = порядок списка, иначе прогоны не сравнить |
| Вывел семь отдельных JSON-объектов | Результат прогона — одна таблица |
| Склонировал `tracker-data` локально | Запись идёт через GitHub API, коммит создаётся на стороне GitHub |
| Положил `—` и `да` в JSON | Оформление — только для markdown-таблицы, в файле числа и `null`/`true` |
| Получил `422` при повторном прогоне за день | Файл уже есть: передай его `sha`, чтобы перезаписать |
| Показал таблицу, умолчав про упавшую запись | Провал сохранения озвучивается явно |
| Сохранил прогон, а потом стал искать предыдущий | Сравнение — до записи, иначе сегодняшний файл затрёт базу сравнения |
| Взял предыдущим прогоном сегодняшний же файл | Файл за текущую дату исключается из поиска |
| Сопоставил товары по имени | Сопоставление по `url`: имя — просто подпись |
| Вывалил всю таблицу вместо пустого diff | «Значимых изменений нет» — это полноценный ответ |
| Сообщил про изменение на 0.3% | Пороги живут в `KNOWLEDGE.md`, читай их каждый прогон |
| Выкинул незначимые товары из JSON | Фильтруется отчёт, история пишется целиком |
| Отправил сводку до сохранения прогона | Сначала история, потом уведомление |
| Отправил по сообщению на каждый товар | Одна сводка на прогон |
| Напечатал токен в лог | Токен живёт в `.env`, в вывод не попадает |
| Промолчал, когда изменений нет | Сводка уходит каждый прогон, в том числе пустая |
